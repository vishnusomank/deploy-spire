apiVersion: v1
kind: PersistentVolume
metadata:
  name: spire-mysql-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 200Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/lib/data"